How to Use

1. import: 
const midTrack = require("./src/modules/mid_track");

2. Init (one time)
midTrack.init();

3. Track:

ex: 
midTrack.track("re-connect", (payload) => {
  console.log("track", payload?.status);
});

topic: gps, lost-gps, has-gps, vConnect, lost-connect, re-connect
